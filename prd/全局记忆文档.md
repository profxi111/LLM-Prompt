# 记忆文档

---

## 文档作用定义

本文档为**开发过程知识沉淀**，用于记录决策、修改、进度快照。服务对象是开发过程中的上下文恢复——当你间隔一段时间后回到项目、启动新的 sub-agent、或者需要排查跨模块问题时，加载此文档快速了解"过去做了什么、为什么这样设计、全局发生过哪些重要变更"。

> 本文档与 `prd/计划文档.md` 独立存在。记忆文档记录"过去"，计划文档指导"接下来怎么干"。

---

### [D-008] V2 Agent 架构：为什么用管道编排而非单一 Agent

**日期**：2026-03-30
**决策**：V2 采用主 Agent + 5 个专项子 Agent 管道编排，不使用单一 Agent 多能力输出

**理由**：

1. **职责单一**：每个 Agent 只做一件事（S1理解图片/S2生成草稿/S3整理输出），边界清晰，用户和开发都可预期
2. **上下文可追溯**：ContextContainer 记录每个 Agent 的输入输出，可查看、可回退
3. **用户反馈机制**：用户可指定任意 Agent 重新处理，实现多轮调整而不丢失上下文
4. **并行检索**：主 Agent 在子 Agent 执行前并行触发 RAG + 知识库检索，不阻塞主流程
5. **可测试性**：每个 Agent 独立单元测试，无需模拟完整流程

**备选方案**：
- 单一 Agent（Claude Artifacts 模式）：单次调用输出所有结果 → 问题在于无法选择性重试、无法可视化链路
- Chain-of-Thought：多步推理但不拆分 Agent → 问题在于上下文截断风险高

**后续影响**：所有子 Agent 遵循 BaseAgent 抽象基类，MasterAgent 统一管理模型路由和上下文传递。

---

### [D-009] 为什么知识库采用纯向量检索而非 LLM 分类

**日期**：2026-03-30
**决策**：K1 提示词分类 Agent 纯用 FAISS 向量检索，无 LLM 调用

**理由**：

1. **零成本**：分类是高频操作，每次上传 md 文件都调用 LLM 成本高
2. **速度快**：向量检索毫秒级，无需等待 LLM 生成
3. **可解释**：Top-1 匹配的示例直接展示，用户可判断分类是否正确
4. **与 RAG 共用基础设施**：embedding service + FAISS 已存在，无需引入额外依赖

**后续影响**：若分类准确率不足，可引入 rerank（两阶段：向量检索 → LLM 判断）。

---

### [D-010] 为什么 ContextContainer 用 JSON 序列化而非关系表

**日期**：2026-03-30
**决策**：ContextContainer 整体序列化为 JSON 存入 sessions 表，不拆分为多个关联表

**理由**：

1. **实现简单**：无需设计复杂的多表关联，一次序列化/反序列化搞定
2. **版本兼容**：ContextContainer 结构变更时，只需修改序列化格式，DB schema 不变
3. **足够高效**：单用户场景下，session 数据量有限，JSON 解析开销可忽略
4. **调试友好**：session JSON 可直接打印查看，便于排查问题

**后续影响**：若未来需要多用户协作或 session 数据量巨大，再考虑拆分。

---

## 一、决策日志（Decision Log）

> 格式：`[D-NNN] 决策标题 —— 日期`
> 每次关键架构决策后追加一条，格式固定，便于追溯。

---

### [D-001] 为什么用 SQLite 而非 PostgreSQL

**日期**：2026-03-27
**决策**：V1.0 采用 SQLite 作为唯一关系型数据库，不引入 PostgreSQL
**理由**：

1. **零配置**：SQLite 为 Python 自带模块（`import sqlite3`），无需安装数据库服务进程
2. **无显卡/低配置友好**：SQLite 纯文件操作，不额外占用内存
3. **单用户场景契合**：V1.0 定位为单机/局域网单用户工具，无并发写入需求
4. **迁移平滑**：SQLite 文件可导出为 CSV，后续如需升级 PostgreSQL 只需改写连接层

**后续影响**：若 V2.0 需要多用户并发支持，需将数据层从 SQLite 迁移至 PostgreSQL，届时 `backend/database/db.py` 的连接管理需要重构。

---

### [D-002] 为什么选 FAISS-CPU 而非 Chroma

**日期**：2026-03-27
**决策**：RAG 向量库采用 `faiss-cpu` 而非 Chroma
**理由**：

1. **无外部依赖**：Chroma 需要独立服务进程（`chroma run`），增加部署复杂度；FAISS 纯 Python 库，导入即用
2. **CPU 原生支持**：FAISS-CPU 专为 CPU 场景优化，Chroma 默认面向有 Embedding 服务的环境
3. **资源占用低**：Chroma 服务常驻内存，FAISS 轻量很多，适配 4GB 低配设备
4. **与 sentence-transformers 生态契合**：两者均为 Meta（Facebook）出品，原生集成更顺畅

**后续影响**：Chroma 的 metadata 过滤能力比 FAISS 更强，若后续 RAG 场景需要按分类/标签过滤，向量库可能需要升级为 Chroma 或 Qdrant。

---

### [D-003] 为什么选 m3e-small 作为嵌入模型

**日期**：2026-03-27
**决策**：向量嵌入采用 `sentence-transformers` 的 `m3e-small` 模型
**理由**：

1. **CPU 优先**：m3e-small 专为 CPU 推理优化，初始化约 200MB，推理 < 1秒，无 GPU 依赖
2. **中文语义友好**：m3e 是专为中文语义设计的模型，在中文电商/海报场景下比英文模型效果更好
3. **稳定版推荐**：m3e-small 是稳定版本，m3e-base/m3e-large 可按需升级
4. **输出维度适中**：768 维向量，FAISS IndexFlatIP 检索速度与精度平衡

**备选方案**：

- `text2vec-base-chinese`（也支持中文，维度 768，Ch senteval 评测相近）
- 后续如需英文场景，可考虑 `all-MiniLM-L6-v2`（维度 384，更快）

**后续影响**：当前无定期模型更新计划，嵌入模型更换时需重建 FAISS 索引（从 SQLite prompts.content 重新向量化）。

---

### [D-004] 为什么用类封装而非函数封装实现模型适配层

**日期**：2026-03-27
**决策**：模型适配层采用 `ModelAdapter` 基类 + 各厂商子类，不使用单一函数 + vendor 参数分支
**理由**：

1. **扩展性**：新增厂商只需新建子类，无需修改主逻辑
2. **封装性**：各厂商的 API 格式差异（header、body、error handling）封装在子类内部
3. **可测试性**：子类可独立 Mock 测试，无需真实 API

**后续影响**：适配器模式已确定，后续新增厂商（Claude、Kimi 等）均遵循此模式。

---

### [D-005] 为什么用懒加载 + 增量索引而非预加载

**日期**：2026-03-27
**决策**：FAISS 索引采用懒加载（启动不预加载，收藏时增量写入），不预构建完整向量库
**理由**：

1. **冷启动快**：预加载全部向量在用户量增长后耗时严重，影响启动体验
2. **与收藏逻辑同步**：用户收藏 → 生成向量 → 写入 FAISS，流程天然一致
3. **低配友好**：4GB 内存设备预加载大量向量可能导致 OOM

**后续影响**：懒加载意味着首次检索需要等待向量写入；后续若检索延迟高，可考虑定时任务在低峰期批量重建索引。

---

### [D-006] 为什么图片用文件上传而非 base64 直传

**日期**：2026-03-27
**决策**：前端 multipart/form-data 上传图片至后端，后端存磁盘，数据库存路径字符串
**理由**：

1. **API body 大小限制**：base64 膨胀 33%，易超网关限制；文件上传 body 极小
2. **数据库体积**：路径字符串 vs base64 字符串，差距数十倍
3. **vision API 集成自然**：后端直接读磁盘文件发给视觉模型
4. **前后端职责清晰**：前端只负责传文件，后端负责存储和调用

**后续影响**：所有涉及图片的 API 均传路径字符串（如 `"uploads/uuid.jpg"`），不传 base64。vision 适配器读取磁盘文件。

---

### [D-007] 为什么视觉模型用适配器模式而非硬编码

**日期**：2026-03-27
**决策**：视觉模型（vision API）使用独立适配器层，与文本模型平级，可插拔
**理由**：

1. **vision 模型选型未定**：通义VL / GPT-4o / 其他随时可换，适配器屏蔽差异
2. **与文本模型架构一致**：ModelAdapter（文本）+ VisionAdapter（视觉）双轨并行
3. **扩展性**：新增视觉模型只需新建子类，无需改动主逻辑

**后续影响**：`backend/adapters/vision/` 目录与 `backend/adapters/` 平级，各厂商子类按需实现 `VisionAdapter.describe(image_path)` 接口。

---

## 二、全局修改记录

> 涉及多文件、多模块的改动在此记录，防止后续遗漏同步更新。

**记录格式**：

```
日期 | 涉及模块 | 修改内容 | 影响范围 | 记录人
```

| 日期 | 涉及模块 | 修改内容 | 影响范围 | 记录人 |
|------|---------|---------|---------|-------|
| 2026-03-27 | prd/记忆文档 | 更新决策日志：新增 D-006（图片上传方案）、D-007（视觉适配器架构）；填入所有决策日期 | M1~M7 各模块均受影响（图片处理流程 + 视觉适配层新增） | — |
| 2026-03-27 | backend/adapters/vision | 新增视觉适配器基类和通义千问VL适配器 | 视觉模型功能 | — |
| 2026-03-27 | backend/services/vision.py | 新增视觉模型服务，实现全局单例和图片描述功能 | 视觉模型功能 | — |
| 2026-03-27 | backend/database/migrations.py | 新增 vision_models 表，用于存储视觉模型配置 | 数据库 | — |
| 2026-03-27 | backend/main.py | 新增视觉模型API接口（GET/POST/DELETE /api/vision-models） | 后端API | — |
| 2026-03-27 | backend/agents/master.py | 集成 VisionService，修改意图识别逻辑使用图片描述 | MasterAgent | — |
| 2026-03-27 | frontend/index.html | 新增"👁️ 视觉模型"按钮和配置界面 | 前端界面 | — |
| 2026-03-27 | frontend/app.js | 新增视觉模型配置功能和粘贴截图功能 | 前端交互 | — |
| 2026-03-27 | 数据库 | 重新初始化数据库，添加 vision_models 表 | 数据库 | — |
| 2026-03-30 | backend/agents/ | 新增 V2 Agent 体系：context.py、base.py、sub/ 目录（6个Agent文件） | Agent 层全部重写 | — |
| 2026-03-30 | backend/agents/master.py | 完全重写：V2 路由逻辑 + 管道编排 + process_adjust | 核心文件变更 | — |
| 2026-03-30 | backend/agents/poster.py, ecommerce.py | 删除：被 V2 S2/S3 替代 | V1 文件删除 | — |
| 2026-03-30 | backend/database/migrations.py | 新增 3 张表：sessions、agent_model_overrides、knowledge_base_meta | 数据库 | — |
| 2026-03-30 | backend/routes/ | 新增 adjust.py、sessions.py、knowledge.py | 新增 API 层 | — |
| 2026-03-30 | backend/services/knowledge_base.py | 新增：KnowledgeBaseService 单例，支持 KB 检索和索引重建 | 知识库服务 | — |
| 2026-03-30 | data/knowledge_base/ | 新增：kb_categories.json + kb_meta.json + kb_examples/（6个JSONL文件，初始示例） | 知识库数据 | — |
| 2026-03-30 | frontend/index.html | 新增：调整工具栏 HTML + 变体选择弹窗 | 前端界面 | — |
| 2026-03-30 | frontend/app.js | 新增：adjust API 调用、Agent链路渲染、变体选择逻辑 | 前端交互 | — |
| 2026-03-30 | frontend/style.css | 新增：调整工具栏 + Agent链路图 + 变体弹窗 CSS | 前端样式 | — |

---

## 三、阶段快照

> 每个里程碑（M1-M7）完成后记录一次，作为"时间胶囊"，便于后续快速恢复上下文。

### V1.0 各里程碑快照

### M1 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 创建完整的项目目录结构（backend/adapters, backend/agents, backend/services, backend/database, backend/utils, frontend, data, uploads）
- 创建 requirements.txt 依赖清单
- 创建 FastAPI 服务入口 main.py
- 初始化 SQLite 数据库（4张核心表：prompts, users, models, logs）
- 验证服务可启动（uvicorn backend.main:app --host 0.0.0.0 --port 8000）
**未解决的问题**：
- sentence-transformers 依赖安装失败（权限问题），已改用简单哈希作为备选方案
**下一步**：
- 进入 M2 数据层开发

### M2 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 实现向量嵌入服务（embedding.py），支持 m3e-small 模型，未安装时使用简单哈希
- 实现 RAG 检索服务（rag.py），基于 FAISS-CPU，支持懒加载和增量索引
- 创建配置管理工具（config.py），定义所有系统配置项
**未解决的问题**：
- 无
**下一步**：
- 进入 M3 模型适配层开发

### M3 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 实现 ModelAdapter 基类和工厂模式（base.py）
- 实现通义千问适配器（qwen.py）
- 实现 DeepSeek 适配器（deepseek.py）
- 实现 MiniMax 适配器（minimax.py）
**未解决的问题**：
- 无
**下一步**：
- 进入 M4 主Agent调度层开发

### M4 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 实现主Agent调度层（master.py）
- 实现意图识别功能（电商/海报/其他场景判断）
- 实现子Agent智能分发
- 实现模型路由和API密钥加密解密
- 实现RAG上下文获取和日志记录
**未解决的问题**：
- 无
**下一步**：
- 进入 M5 专项子Agent层开发

### M5 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 实现电商Agent（ecommerce.py），生成产品标题、卖点、详情页、营销文案
- 实现海报Agent（poster.py），生成构图、色彩、光影、风格、镜头、元素搭配
- 支持RAG上下文和用户偏好
**未解决的问题**：
- 无
**下一步**：
- 进入 M6 前端交互层开发

### M6 完成后快照
**完成时间**：2026-03-27
**完成内容**：
- 创建前端 HTML 页面（index.html），包含所有功能模块
- 创建前端样式（style.css），响应式设计
- 创建前端交互逻辑（app.js），实现所有API调用
- 添加后端 API 路由（main.py）：上传、生成、收藏、检索、设置、模型管理
**未解决的问题**：
- 无
**下一步**：
- M7 RAG检索集成（已在M6中完成，可作为可选优化项）

### M7 完成后快照（待填充）
**完成时间**：—
**完成内容**：
- [待填充]
**未解决的问题**：
- [阻塞项或待优化项]
**下一步**：
- [进入下一阶段前需要确认的事]

### V2.0 Agent 体系重构快照
**完成时间**：2026-03-30
**完成内容**：

**Phase 1 — P0 核心 Agent 重构**
- `backend/agents/context.py` — ContextContainer 核心类（含截断策略、序列化、AgentOutput 记录）
- `backend/agents/base.py` — BaseAgent 抽象基类
- `backend/agents/sub/s1_image_understand.py` — S1 图片理解 Agent（调用 vision_service）
- `backend/agents/sub/s2_composition_gen.py` — S2 构图生成 Agent（核心节点，不做整理翻译）
- `backend/agents/sub/s3_organize.py` — S3 整理优化 Agent（终端节点，中英双语 + 字数控制）
- `backend/agents/sub/s4_style_extend.py` — S4 风格延展 Agent（生成3个变体）
- `backend/agents/sub/s5_video_shot.py` — S5 视频空镜 Agent（6秒科技空镜头）
- `backend/agents/sub/k1_classifier.py` — K1 分类 Agent（纯向量检索，无LLM）
- `backend/agents/master.py` — 完全重写：V2 路由（S1_S2_S3 / S2_S3 / S5_S3 / K1）+ process_adjust + 模型路由 + session 管理
- `backend/agents/poster.py` + `backend/agents/ecommerce.py` — 删除

**Phase 1 — P0 数据库变更**
- `backend/database/migrations.py` — 新增 sessions、agent_model_overrides、knowledge_base_meta 3张表

**Phase 1 — P1 前端 + 路由**
- `backend/routes/adjust.py` — POST /api/adjust
- `backend/routes/sessions.py` — GET/DELETE /api/sessions
- `backend/routes/knowledge.py` — GET /api/knowledge/categories、POST /api/knowledge/classify、POST /api/knowledge/rebuild
- `frontend/index.html` — 新增调整工具栏 + Agent链路图 + 变体选择弹窗
- `frontend/style.css` — 新增工具栏和弹窗样式
- `frontend/app.js` — 新增 adjust API 调用、链路渲染、变体选择

**Phase 2 — P2 知识库**
- `data/knowledge_base/kb_categories.json` — 6个分类定义
- `data/knowledge_base/kb_meta.json` — 版本信息
- `data/knowledge_base/kb_examples/` — 6个JSONL文件（科技风8条、复古风8条、国潮风8条、视频空镜头6条、电商标题5条、通用5条）
- `backend/services/knowledge_base.py` — KnowledgeBaseService 单例（search / classify / add_example / rebuild_index）

**启动前必须操作**：
```bash
cd 开发仓库/LLM-Prompt
# 首次启动时服务会自动初始化数据库（新增3张表）
# 知识库向量索引在首次调用时自动构建（POST /api/knowledge/rebuild）
uvicorn backend.main:app --reload --port 8000
```

**未解决的问题**：
- Agent 模型覆盖配置界面（前端 P3 阶段）
- 上下文截断前端提示（已内置逻辑，需测试验证）

**下一步**：
- 启动服务验证 P0 功能（图片+文字 S1→S2→S3、纯文字 S2→S3、调整工具栏）
- 调用 POST /api/knowledge/rebuild 初始化知识库向量索引
- 测试 /api/adjust 调整流程

---

## 四、踩坑记录

> 开发过程中遇到的技术难点及解决方案，供后续参考。

**记录格式**：

```
### 问题N：[问题标题]
**日期**：YYYY-MM-DD
**问题描述**：
**原因分析**：
**解决方案**：
**防止复发的措施**：
```

### 问题1：前端按钮无法点击
**日期**：2026-03-27
**问题描述**：
前端界面显示正常，但所有按钮点击都没有反馈，无法进行任何操作。

**原因分析**：
1. HTML中使用了相对路径引用静态文件（`style.css`、`app.js`）
2. FastAPI的静态文件挂载在 `/static/` 路径下（`app.mount("/static", StaticFiles(directory="frontend")`）
3. 浏览器请求相对路径时返回404错误，CSS和JS文件无法加载
4. 没有CSS导致样式异常，没有JS导致事件监听器未绑定

**解决方案**：
修改 `frontend/index.html` 中的静态文件引用路径：
- `<link rel="stylesheet" href="style.css">` → `<link rel="stylesheet" href="/static/style.css">`
- `<script src="app.js"></script>` → `<script src="/static/app.js"></script>`

**验证方法**：
1. 打开浏览器开发者工具（F12）
2. 切换到Console标签页
3. 查看是否有404错误
4. 切换到Network标签页
5. 检查style.css和app.js的状态码是否为200

**防止复发的措施**：
在开发规范中约定：所有静态文件引用必须使用 `/static/` 前缀，禁止使用相对路径。

---

### 问题2：视觉模型独立服务架构
**日期**：2026-03-27
**问题描述**：
用户需要分别配置文本模型和视觉模型，视觉模型应该作为全局服务，供所有Agent使用，而不是绑定到MasterAgent。

**原因分析**：
1. 原设计将视觉模型集成在MasterAgent中，只服务于意图识别
2. 其他Agent（EcommerceAgent、PosterAgent）无法使用视觉模型
3. 视觉模型职责不清晰，耦合度高

**解决方案**：
采用视觉模型独立服务架构：
1. 创建 `backend/services/vision.py` - VisionService全局单例
2. 创建 `backend/adapters/vision/` 目录，实现视觉模型适配器
3. 修改 MasterAgent，集成 VisionService
4. 新增 `vision_models` 数据库表，独立存储视觉模型配置
5. 新增视觉模型API接口和前端配置界面

**架构优势**：
- ✅ 解耦：视觉模型不绑定到任何Agent
- ✅ 全局可用：任何Agent都可以调用
- ✅ 灵活：可以根据需要选择是否使用视觉模型
- ✅ 易维护：视觉模型配置集中管理
- ✅ 易扩展：新增视觉模型只需修改VisionService

**实现内容**：
- backend/adapters/vision/base.py - VisionAdapter基类
- backend/adapters/vision/qwen_vl.py - 通义千问VL适配器
- backend/services/vision.py - VisionService全局服务
- backend/database/migrations.py - 新增vision_models表
- backend/main.py - 新增视觉模型API接口
- backend/agents/master.py - 集成VisionService
- frontend/index.html - 新增视觉模型配置界面
- frontend/app.js - 新增视觉模型配置功能

**防止复发的措施**：
视觉模型采用独立服务架构，与文本模型平级，职责清晰。

---

### 问题3：（待补充）
后续开发中遇到的问题在此追加...

---

## 五、进度看板

> 轻量表格，随开发进度实时更新。状态：TODO / In Progress / Done / Blocked

### 里程碑进度

| 里程碑 | 状态 | 开始时间 | 预计完成 | 实际完成 | 备注 |
|--------|------|---------|---------|---------|------|
| M1：环境搭建 | In Progress | 2026-03-27 | — | — | 刚启动 |
| M2：数据层 | TODO | — | — | — | 依赖 M1 |
| M3：模型适配层 | TODO | — | — | — | 依赖 M2 |
| M4：主Agent调度层 | TODO | — | — | — | 依赖 M2、M3 |
| M5：专项子Agent层 | TODO | — | — | — | 依赖 M4 |
| M6：前端交互层 | TODO | — | — | — | 可与 M4/M5 并行开发 |
| M7：RAG检索集成 | TODO | — | — | — | 依赖 M2、M4、M6 |

### 模块详细进度
| 模块 | 子模块 | 状态 | 备注 |
|------|-------|------|------|
| backend/adapters | base.py | Done | — |
| backend/adapters | qwen.py | Done | — |
| backend/adapters | deepseek.py | Done | — |
| backend/adapters | minimax.py | Done | — |
| backend/agents | master.py | Done | V2 完全重写（路由+编排+adjust+session） |
| backend/agents | context.py | Done | V2 新增，ContextContainer 核心类 |
| backend/agents | base.py | Done | V2 新增，BaseAgent 抽象基类 |
| backend/agents | ecommerce.py | Done | **已删除**，被 V2 S2/S3 替代 |
| backend/agents | poster.py | Done | **已删除**，被 V2 S2/S3 替代 |
| backend/agents/sub | s1_image_understand.py | Done | V2 新增 |
| backend/agents/sub | s2_composition_gen.py | Done | V2 新增（核心节点） |
| backend/agents/sub | s3_organize.py | Done | V2 新增（终端节点） |
| backend/agents/sub | s4_style_extend.py | Done | V2 新增 |
| backend/agents/sub | s5_video_shot.py | Done | V2 新增 |
| backend/agents/sub | k1_classifier.py | Done | V2 新增 |
| backend/services | rag.py | Done | — |
| backend/services | embedding.py | Done | 支持m3e-small和简单哈希备选方案 |
| backend/services | knowledge_base.py | Done | V2 新增 |
| backend/routes | adjust.py | Done | V2 新增 |
| backend/routes | sessions.py | Done | V2 新增 |
| backend/routes | knowledge.py | Done | V2 新增 |
| backend/database | db.py | Done | — |
| backend/database | models.py | Done | — |
| backend/database | migrations.py | Done | V2 新增3张表 |
| frontend | index.html | Done | V2 新增工具栏+弹窗 |
| frontend | style.css | Done | V2 新增样式 |
| frontend | app.js | Done | V2 新增adjust+链路+变体逻辑 |

---

## 六、运维操作指南

> 记录服务的启动、停止、性能监控等运维操作，便于日常开发和部署使用。

### 6.1 启动服务（V2 版本）

> **V2 版本新增**：首次启动时会自动初始化数据库（新增 sessions、agent_model_overrides、knowledge_base_meta 3张表）。知识库向量索引需手动触发构建（见 6.6 节）。

**macOS / Linux 通用**：
```bash
cd 开发仓库/LLM-Prompt

# 启动服务
uvicorn backend.main:app --reload --port 8000
```

**服务信息**：
- 默认端口：8000
- 访问地址：http://localhost:8000/
- 局域网访问：http://[本机IP]:8000/
- API 文档：http://localhost:8000/docs
```powershell
# 设置Python路径（解决模块导入问题）
$env:PYTHONPATH="d:\trae test"

# 启动服务
python backend/main.py
```

**或使用 uvicorn**（推荐）：
```powershell
$env:PYTHONPATH="d:\trae test"
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

**服务信息**：
- 默认端口：8000
- 访问地址：http://localhost:8000/
- 局域网访问：http://[本机IP]:8000/

### 6.2 停止服务

**方法1：Ctrl+C 终止**
- 在运行服务的终端窗口按 `Ctrl + C`

**方法2：查找并终止进程**
```powershell
# 查找占用8000端口的进程
netstat -ano | findstr :8000

# 终止指定PID的进程（替换21912为实际PID）
taskkill /F /PID 21912
```

**方法3：终止所有Python进程**
```powershell
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 6.3 性能监控

**查看进程资源占用**：
```powershell
# 查看Python进程的CPU、内存、线程数
Get-Process -Name python | Select-Object Id, ProcessName, CPU, @{Name='内存(MB)';Expression={[math]::Round($_.WorkingSet/1MB,2)}}, @{Name='线程数';Expression={$_.Threads.Count}}
```

**查看网络连接**：
```powershell
# 查看8000端口的活跃连接
netstat -ano | findstr :8000 | findstr ESTABLISHED
```

**性能基准参考**（V1.0版本）：
- CPU使用率：3%左右（空闲状态）
- 内存使用：90MB左右
- 线程数：33个
- 活跃连接：根据用户数量动态变化

### 6.4 常见问题排查

**问题1：ModuleNotFoundError: No module named 'backend'**
- **原因**：Python路径未正确设置
- **解决**：设置环境变量 `$env:PYTHONPATH="d:\trae test"`

**问题2：[Errno 10048] 端口已被占用**
- **原因**：8000端口已被其他进程占用
- **解决**：查找并终止占用进程（见6.2节）

**问题3：前端按钮无法点击**
- **原因**：静态文件路径配置错误（404错误）
- **解决**：确保HTML中使用 `/static/` 前缀引用CSS和JS文件

**问题4：前端样式异常**
- **原因**：CSS文件未加载
- **解决**：检查浏览器控制台是否有404错误，确认路径为 `/static/style.css`

### 6.5 重建知识库向量索引（V2 新增）

> 知识库初始示例在 `data/knowledge_base/kb_examples/` 中，向量索引需手动构建后 RAG 检索才能生效。

**方法1：API 调用（推荐）**
```bash
# 启动服务后执行
curl -X POST http://localhost:8000/api/knowledge/rebuild
```

**方法2：代码调用**
```python
from backend.services.knowledge_base import get_knowledge_base_service
kb = get_knowledge_base_service()
kb.rebuild_index()
```

**构建内容**：
- 遍历 `data/knowledge_base/kb_examples/*.jsonl`（6个分类文件）
- 每条示例调用 m3e-small 生成 768维向量
- 写入 `data/knowledge_base/kb_vectors/examples.index`
- 同时生成 `examples_meta.jsonl` 元数据

**何时需要重建**：
- 首次部署时
- 添加新示例后
- 切换 embedding 模型后

---

### 6.6 开发环境配置

**Python版本要求**：3.10 ~ 3.11

**依赖安装**：
```bash
pip install -r requirements.txt
```

**核心依赖**：
- fastapi - Web框架
- uvicorn - ASGI服务器
- faiss-cpu - 向量检索（CPU版）
- sentence-transformers - 文本嵌入（可选，未安装时使用简单哈希）
- cryptography - API密钥加密
- requests - HTTP请求
- python-multipart - 文件上传支持

### 6.7 V2 新增 API 速查

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/adjust` | 用户调整：body含 session_id + target_agent + user_instruction |
| GET | `/api/sessions` | 列出最近20条会话 |
| GET | `/api/sessions/{session_id}` | 获取完整 ContextContainer（含链路详情） |
| DELETE | `/api/sessions/{session_id}` | 删除会话 |
| GET | `/api/knowledge/categories` | 获取知识库分类列表 |
| POST | `/api/knowledge/classify` | 对多行文本进行分类（K1流程） |
| POST | `/api/knowledge/rebuild` | 重建知识库向量索引 |
| POST | `/api/knowledge/examples` | 向知识库追加示例 |

---

## 七、版本记录
| 版本 | 日期 | 更新内容 | 记录人 |
|------|------|---------|-------|
| v0.1 | 计划阶段 | 初始化文档，建立决策日志、进度看板框架 | — |
| v1.0 | 2026-03-27 | 完成V1.0所有核心功能开发，包括环境搭建、数据层、模型适配层、主Agent调度层、专项子Agent层、前端交互层 | — |
| v2.0 | 2026-03-30 | 完成V2 Agent体系重构：主Agent重写 + 5个专项子Agent + K1分类器 + ContextContainer + 知识库服务 + 会话管理 + 调整工具栏前端；删除 ecommerce.py/poster.py | — |

---

## 八、调试日志（2026-03-30 晚间调试）

### 调试背景
用户反馈：发送"你好"时返回"生成失败: undefined"

### 调试过程

**1. 定位问题方向**
- 直接调用MiniMax API适配器成功（2-3秒响应）
- 通过MasterAgent调用时挂起
- 怀疑：嵌入模型(m3e-small)加载失败导致RAG流程异常

**2. 实施的修复**
- 修改 `_get_rag_context`：当嵌入模型未加载时跳过RAG检索
- 添加 `_simple_identify_intent`：基于关键词的简单意图识别
- 修改 `_identify_intent`：优先使用简单意图识别（置信度>=0.9直接返回）

**3. 发现的问题**
- 嵌入模型加载失败：sentence-transformers未正确安装，尝试连接HuggingFace失败
- **发现MiniMax API响应解析问题**：当max_tokens较小时，响应只返回thinking内容，没有text类型
- 通过FastAPI TestClient直接测试可以成功调用，但通过HTTP服务器请求会超时无响应

**4. 待解决的问题**
- **核心问题**：通过HTTP请求调用/api/generate时请求挂起无响应
- 可能原因：uvicorn服务器进程处理请求时存在某种阻塞
- MiniMax API适配器需要优化：处理只有thinking没有text的响应

### 调试中创建的测试文件（可删除）
- test_*.py（多个测试脚本）

---

## 九、待办计划（TODO）

### 优先级P0 - 阻塞性问题

| 序号 | 任务 | 描述 | 状态 |
|------|------|------|------|
| 1 | 修复API请求挂起 | 通过HTTP请求调用/api/generate时无响应超时 | **待解决** |
| 2 | MiniMax响应解析优化 | 处理只有thinking没有text的API响应 | 待优化 |

### 优先级P1 - 重要功能

| 序号 | 任务 | 描述 | 状态 |
|------|------|------|------|
| 3 | 嵌入模型安装 | 安装sentence-transformers并下载m3e-small模型 | 待完成 |
| 4 | 前端测试验证 | 在浏览器中测试完整生成流程 | 待测试 |

### 优先级P2 - 优化项

| 序号 | 任务 | 描述 | 状态 |
|------|------|------|------|
| 5 | 清理测试文件 | 删除调试过程中创建的test_*.py文件 | 待清理 |
| 6 | 日志优化 | 移除生产环境中的DEBUG日志输出 | 待优化 |

---

### 下一步行动建议

1. **优先排查API挂起问题**：
   - 检查uvicorn日志配置
   - 尝试使用不同的ASGI服务器（如hypercorn）
   - 检查Windows环境下的网络代理设置

2. **快速验证功能**：
   - 使用FastAPI TestClient直接测试（已验证可用）
   - 确认问题范围是否仅在HTTP服务器场景

3. **备选方案**：
   - 如果API挂起问题短期内无法解决，考虑提供一个不依赖外部LLM的简化版本
   - 或使用本地模型（如Ollama）作为备选
